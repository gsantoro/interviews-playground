[project]
name = "incident-io-take-home"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest==8.3.4",
    "python-dateutil==2.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
